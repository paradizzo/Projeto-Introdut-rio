/*
 * This Java source file was generated by the Gradle 'init' task.
 */
package TodoListProject.app;

import TodoListProject.list.LinkedList;

import static TodoListProject.utilities.StringUtils.join;
import static TodoListProject.utilities.StringUtils.split;
import static TodoListProject.app.MessageUtils.getMessage;

import org.apache.commons.text.WordUtils;

public class App {
    public static void main(String[] args) {
        LinkedList tokens;
        tokens = split(getMessage());
        String result = join(tokens);
        System.out.println(WordUtils.capitalize(result));
    }
}
